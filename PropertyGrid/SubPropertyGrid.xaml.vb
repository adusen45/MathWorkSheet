Public Class SubPropertyGrid

    Private Sub UserControl_Loaded(sender As Object, e As RoutedEventArgs)
        'Dim skye = DataContext.GetType.GetProperties
        'pGrid.DataContext = CType(Me.DataContext, [Property]).Accessor

    End Sub
End Class
